add user => {
    accNum:Date.now(),
    name:"ahmed",
    balance:1000,
    transactions: []
}

findIndex = 2
allUsers =[user, .....]
tranDetails= {type:'add', val:100}
<option value="add">add</option>
allUsers[2].transactions.push(tranDetails)
if(tranDetails.type=="add") allUsers[2].balance+=tranDetails.val